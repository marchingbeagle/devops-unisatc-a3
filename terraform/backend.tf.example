# Example Terraform backend configuration for S3
# Uncomment and configure if you want to use remote state storage
# This is optional - local state works fine for demos

# terraform {
#   backend "s3" {
#     bucket         = "your-terraform-state-bucket"
#     key            = "devops-strapi/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "terraform-state-lock"  # Optional: for state locking
#   }
# }

# To use this backend:
# 1. Create an S3 bucket for storing Terraform state
# 2. Optionally create a DynamoDB table for state locking
# 3. Copy this file to backend.tf
# 4. Update the bucket name and other values
# 5. Run terraform init

